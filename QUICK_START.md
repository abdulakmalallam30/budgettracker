# Quick Start Guide - Expense Category Analyzer

## 🚀 Quick Setup (3 Steps)

### Step 1: Install Backend Dependencies
```powershell
cd backend
npm install
```

### Step 2: Install Frontend Dependencies
```powershell
cd ../frontend
npm install
```

### Step 3: Run the Application

**Terminal 1 - Start Backend:**
```powershell
cd backend
npm start
```
✅ Backend running at: http://localhost:5000

**Terminal 2 - Start Frontend:**
```powershell
cd frontend
npm start
```
✅ Frontend opens at: http://localhost:3000

## 📊 Try It Out!

1. Use the sample file: `sample-expenses.csv`
2. Upload it via the dashboard
3. Watch the magic happen! 🎉

## 🆘 Need Help?

### Backend won't start?
- Make sure you're in the `backend` folder
- Run: `npm install` again
- Check if port 5000 is available

### Frontend won't start?
- Make sure you're in the `frontend` folder
- Run: `npm install` again
- Check if port 3000 is available

### CSV upload failing?
- Ensure columns are: Date, Description, Amount, Mode
- Check date format: YYYY-MM-DD
- Try the sample file first!

---

**Pro Tip:** Keep both terminals open while using the app!
